{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "cross-verification verdict output",
  "type": "object",
  "additionalProperties": false,
  "required": ["verifications"],
  "properties": {
    "verification_mode": {
      "type": "string",
      "enum": ["full_3way", "codex_only", "gemini_only", "skipped"],
      "description": "Populated by the team-agent driver after fallback matrix evaluation — verifier CLIs do not emit this field themselves."
    },
    "verifications": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["finding_id", "verdict", "rationale"],
        "properties": {
          "finding_id": { "type": "string" },
          "verdict": { "type": "string", "enum": ["confirmed", "disagree_severity", "not_an_issue"] },
          "suggested_severity": { "type": "string", "enum": ["Critical", "High", "Medium", "Low", "Info"] },
          "rationale": { "type": "string", "maxLength": 500 },
          "code_quote": { "type": "string", "maxLength": 500 }
        }
      }
    }
  }
}
